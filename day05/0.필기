순서
1.float
2.muliticolimn
3. background
cover : 이미지를 공간안에 꽉채운다


4. 미디어 쿼리
미디어 쿼리는 화면크기에 따른 각각의 속성 값을 지정하여, 여러가지 화면을 구성하는 기술이다.

@media only all and (조건문){실행문}
- @media : 미디어 쿼리가 시작됨을 표시
- only : 미디어 쿼리 구문을 해석하라는 명령어이며 (생략가능)
- all : 미디어 쿼리를 해석해야할 대상을 나타내며 (생략가능)
- and : 앞과 뒤의 조건을 나타낸다(생략가능)
- (조건문) : 해당 조건을 설정, 
                max-width: 이하, min-width: 이상
            의 조건일 때 실행문이 실행되도록함
- {실행문} : 조건에 따라 실행할 스타일 설정

5. link 와 import
- 외부로 파일을 빼는 방법 2가지로 결과는 같다.
- <link rel="stylesheet" href="style.css">
: html태그로 하는 법
- <style>
    @import url("style.css")
</style>
: css로 style태그에서 하는 법

6. cache 
- cache 캐시 는 자주 접근하는 데이터를 복사 해놓는 임시 저장소
- browser cache : 서버 지연을 줄이기 위해 사용하는 웹캐시의 일종

7. bootstrap
헤드부분에 링크를 넣고 바디에 스크립을 복붙해야한다.
무료 사이트
https://themewagon.com/theme-price/free/


--------------------------------------------------CSS끝 & JavaScript시작
확장자가 js 즉,  *.js인 파일

8. 바닐라 스크립트(Vanilla JS)
- 바닐라 스크립트란 플러그인이나, 라이브러리를 사용하지 않은 순수 자바스크립트를 이야기한다.
- 즉 바닐라 스크립트 = 자바스크립트라고 생각하면 된다.

8-1. 자바스크립트에 영향을 준 언어
- java랑은 아무런 연관이 없다.
8-2. 자바스크립트 버전
- 브라우저가 자바스크립트를 내장하고있다. 
- 하지만 사피리, 크롬, IE등등 제조사마다 조금씩 다르다.
- ECMA
- ES6 버전을 탑재하고 있다.

10. 변수의 사용방법
- 변수를 사용하기 위해서는 '선언' 과 '할당'의 두 영역으로 구분해야한다.
- 변수의 선언
    var num;
    var hello;

- 변수의 할당
    num = 12345;
    hello = "안녕하세요";

11. 변수의 종류
- javascript의 변수는 표현하는 값의 종류에 따라서 형식이 구분된다.
- 이러한 변수의 종류를 데이터 형이라고 한다.
    Number(정수, 실수) : 숫자를 표현하거나 산술 연산을 하는데 사용되는 데이터 타입
    String(문자열) : 문자열을 표현하는데 사용되는 타이터 타입. 쌍따옴표나 홑따옴표로 감싸진 문자를 의미한다.
    Boolean(참, 거짓) : true, false값을 가지는 논리 데이터 타입
    Object(객체) : 객체를 저장하기 위한 데이터 타입이다.브라우저 제어기능, Form제어, HTML태그요소에 대한 제어 등이 가능하다.
    Null(값 없음) : 개발자가 의도적으로 변수의 공간을 비워 놓은 상태를 의미한다.
    undefined(정의 되지 않음) : 처음부터 변수가 선언만 되고 아무런 값도 할당되지 않은 상태를 의미한다. 이상태의 변수는 어떠한 처리도 불가능하다.


11-1. Null과 Undefined의 차일점
- java: 변수의 선언시에 자료형이 결정
        객체의 경우 null숫자형태의 변수는 0, boolean은 false로 결정된다.
- javasrctipt : 변수의 선언시에는 변수의 존재 여부와 스코프(범위)만 결정되고 자료형은 값을 할당할 때 자동으로 결정된다.
            자료형이 결정되지 않은 상태이기 때문에 기본값이 할당될 수 없다. 무조건 undifined로 할당된다.
            - 자바스크립트는 할당되지 않은 변수에 대하여 자동을 ㅗ기본값이 적용되지 않는다. 기본값을 설정하기 위해서는 개발자가 직접 값을 할당해 주어야하는데 null은 이 때 사용하는 객체 변수에 대한 기본값으로 사용된다.
            - 즉, null은 기능이 부여되지 않은 비어있는 객체 상태를 의미한다.
            